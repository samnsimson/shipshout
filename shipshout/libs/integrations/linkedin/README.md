# linkedin

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build linkedin` to build the library.

## Running unit tests

Run `nx test linkedin` to execute the unit tests via [Jest](https://jestjs.io).
