# mailchimp

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build mailchimp` to build the library.

## Running unit tests

Run `nx test mailchimp` to execute the unit tests via [Jest](https://jestjs.io).
