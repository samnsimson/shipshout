# email-client

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build email-client` to build the library.

## Running unit tests

Run `nx test email-client` to execute the unit tests via [Jest](https://jestjs.io).
